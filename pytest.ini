[pytest]
markers =
    asr: real local ASR tests requiring separately installed model
    samples: real RU/KK/mixed audio supplied by operator
addopts = -ra
testpaths = tests
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated:DeprecationWarning
