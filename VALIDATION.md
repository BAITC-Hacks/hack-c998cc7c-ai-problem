# Тексеру нәтижелері — 2026-09-23

- Windows, Python 3.12, жергілікті CPU.
- `python -m pytest tests/test_pipeline.py tests/test_audio.py -q`: **42 passed, 3 skipped**.
- Нақты FFmpeg WAV декодтауы, бүлінген MP4 және ұзақтық шегі тексерілді.
- Бөлек жүктелген faster-whisper **tiny**, CPU int8, нақты синтетикалық үнсіз WAV: бос транскрипт. Бұл сөйлеу дәлдігін өлшемейді.
- RU/KK/mixed мәтіндік fixtures: мерзім ауысуы, жою, ұсыныс, белгісіз орындаушы/мерзім, сөйлеуші мен орындаушының айырмашылығы тексерілді.
- Нақты RU/KK/mixed сөйлеу файлдары және эталон мәтіндері жоқ: үш ASR сапа тесті skip. ASR сапасы немесе диаризация дәлдігі өлшенді деп мәлімдемейміз.
- SQLite атомдық claim, үзілістен қалпына келу, revision қақтығысы, өзгермейтін бекітілген snapshot, қайта талдаудағы manual өзгерістердің сақталуы тексерілді.
- Модельдің жоқтығы, LLM қолжетімсіздігі және дұрыс емес JSON / дәлелдер үшін шектелген қайталау тексерілді. Нақты LLM серверімен сәтті талдау бұл ортада тексерілген жоқ.
- HTTP Range `206` және нақты байттар, upload көлемі/форматы, cross-origin write шектеуі, бекітілген нұсқа таңдау тексерілді.
- DOCX python-docx арқылы қайта ашылды. PDF pypdf арқылы оқылып, қазақ әріптері тексерілді; ұзын мәтін бірнеше бетке тасымалданды. PDF бір беттік үлгісі PNG-ге рендерленіп визуалды қаралды.
- DOCX визуалды render: LibreOffice `soffice.exe` жоқ, орындалмады. Word-тағы беттеуін қосымша қарау қажет.
- Браузер: KZ/RU формалары, бөлек UI мәтіндік fixture-де редактор, бос өрісті растау, сақтау, нұсқа бекіту тексерілді. Дәлелді басу audio.currentTime=5 және segment=s2 берді. 1440 px екі баған, 320 px-та көлденең overflow жоқ. Тест деректері `data/ui-test`, негізгі деректерден бөлек.
- JS файлдарының `node --check` тексеруі өтті.
- Кейбір жеке іске қосуларда осы Windows ортасы loopback socket үшін уақытша WinError 10013 берді; қайта іске қосылған толық тексеру жоғарыдағы нәтижемен өтті.
- Starlette TestClient httpx-ке қатысты deprecation warning береді; жұмысқа әсер етпейді.

Осы компьютерде `backend/.env` ASR үшін `tiny` моделін көрсетеді. Бұл жеңіл smoke-test конфигурациясы; `small` жүктеуі аяқталмады. Қажет модельді бөлек орнатып, `WHISPER_MODEL` өзгертіңіз. LOCAL режиміне жергілікті OpenAI-compatible LLM endpoint қажет; LLM-сіз жол үшін жүктеу кезінде RULES таңдаңыз.

