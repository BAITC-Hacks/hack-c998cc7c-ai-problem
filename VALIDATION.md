# Проверка перед push — 2026-09-23

- Полный pytest: **91 passed, 3 skipped**, 16.86 секунды. Пропущены только тесты
  качества речи без ru/kk/mixed аудио и эталонных текстов; исключённых регрессий нет.
- Финальный полный Chromium: **17 passed**, 14.9 секунды, включая проверку всех
  20 сочетаний маршрута/ширины, запоздалый ответ API и штатную очистку тестового сервера.
- `npm run check --prefix frontend` и `git diff --check` проходят.
- `npm audit`: 0 уязвимостей. `pip-audit` проверил 52 установленных пакета,
  известных уязвимостей не найдено. Это проверка базы известных уязвимостей,
  а не гарантия отсутствия новых.
- Регрессии воспроизведены до исправления: неверная привязка цитат ко времени,
  выдуманные говорящие по паузам/тексту, остановка worker после блокировки SQLite,
  повторная отправка формы, потеря данных при смене языка, переполнение таблицы.
- Тестовый сервер изолирован по порту, каталогу, идентификатору и конфигурации API.
  Добавлен GitHub Actions workflow для backend и Chromium без реальных платных API.
- Удалённый CI пока не выполнил ни одного шага: GitHub сообщает
  `The job was not started because your account is locked due to a billing issue.`
  [Запуск с аннотацией](https://github.com/BAITC-Hacks/hack-c998cc7c-ai-problem/actions/runs/35862163188).
  Нужно устранить billing-блокировку владельца аккаунта и повторить workflow.
  Успешными заявлены только локальные проверки; CI не обходится и не отключается.
- `.env`, модели, записи и локальные пароли исключены из Git. Случайно отслеживаемый
  пустой файл кэша удалён из индекса.

Остаются внешние условия: провайдер/модели/ключи для реального API-прогона и разрешённые
речевые записи. Автоматическая акустическая диаризация не подключена; неизвестные
говорящие остаются неизвестными. Локальное приложение и API-интеграция проверены,
точность внешнего ИИ и выделения говорящих не измерена.

## Предыдущий этап: локальный режим с внешними API

- Cloudflare остановлен. Реальный сервер слушает только `127.0.0.1:8000`.
- Health возвращает `public_mode=false`, работающий worker, режимы API/HYBRID.
- Backend: **78 passed, 3 skipped**, 12.34 секунды. Пропущены только тесты качества
  локального ASR без реальных RU/KK/mixed записей. Вызов: `python -m pytest -q
  -p no:cacheprovider --basetemp=.cache/api-final-tests` (отдельный временный каталог
  использован из-за прав Windows на старые тестовые каталоги).
- Новые тесты: Bearer-заголовки, сохранность ключей, preflight, отдельное согласие
  на аудио, сегментация и смещение временных меток, отклонение неверных ответов,
  полный путь upload → FFmpeg → аудио-API → LLM → черновик.
- Браузер: **10 passed**, включая начальную конфигурацию API и передачу обоих согласий.
- Реальные провайдеры не вызывались: их адреса, модели и ключи пока не заданы.
  Тестовые ответы проверяют интеграцию, но не качество и не доступность внешнего ИИ.

## Предыдущий этап: UI и пробный Tunnel (история)

# Проверки — 2026-09-23

Среда: Windows, Python 3.12 в `.venv`, CPU, Chromium Playwright 1.63.0.

## Автоматические проверки

- `.venv/Scripts/python.exe -m pytest -q`: **66 passed, 3 skipped**, 16 секунд.
- `npm run check --prefix frontend`: все пять JS-модулей проходят синтаксическую проверку.
- `npm test --prefix frontend`: **8 passed**, 7.2 секунды, успешное завершение процесса.
  В sandbox Windows для очистки дочерних процессов потребовался запуск с разрешением.
- `git diff --check`: ошибок пробелов нет.

Backend: изменение содержимого снимает подтверждение; правка транскрипта удаляет
устаревший candidate; LLM не может обойти проверку доказательств через origin=manual;
сводки, вопросы и решения накапливаются между фрагментами; проверяется итоговый размер
контекста. Реестр поручений читает черновики одним запросом без истории версий.

Также проверены очередь, ревизии, неизменяемые версии, Range-запросы аудио, ограничения
загрузки, FFmpeg, документы DOCX/PDF, same-origin, Basic Auth, перенаправление HTTPS
с сохранением query, закрытая статика и публичная конфигурация.

Браузер: синхронное снятие галочек при правке; блокировка ввода при медленном сохранении;
сохранность черновика после 409; поиск; переключение языка без потери правок и отмена;
утверждение версии и скачивание DOCX/PDF. Пять экранов проверены при ширине
320/768/1024/1440 без горизонтального overflow страницы и ошибок JS.
Данные браузерных тестов создаются в отдельном временном каталоге.

## Реальный внешний запуск

Cloudflared **2026.9.1**, SHA256 сверена с digest официального GitHub-релиза.
Приложение запущено через Quick Tunnel с обязательным случайным паролем.
Внешние HTTP-проверки: без авторизации `/api/meetings` → 401; с авторизацией
`/`, `/api/health`, `/static/app.js` → 200; `/static/package.json` → 404.
Health подтверждает ASR-модель, FFmpeg, worker и PUBLIC_MODE.

Публичный сайт открыт реальным Chromium с авторизацией: список встреч и форма загрузки
работают, ошибок JS нет, мобильная форма 320 px не выходит за границы экрана.
Скриншоты: `.cache/demo/public-desktop.png`, `.cache/demo/public-mobile.png`.
Адрес и секреты находятся только в `.cache/demo/access.json`; инструкция — DEPLOYMENT.md.

## Границы проверки

- Установлена модель faster-whisper **small**. Реальный прогон на синтетической тишине
  проходит, но он не измеряет качество распознавания речи.
- Три ASR-теста пропущены: отсутствуют согласованные записи ru.wav, kk.wav, mixed.wav
  и эталонные расшифровки. Качество RU/KK/mixed речи пока не измерено.
- Рабочий LLM endpoint не настроен. LOCAL/HYBRID успешно с реальным LLM не проверялись;
  для пробы без ключей следует выбирать RULES. Его извлечение ограничено правилами.
- Автоматической диаризации нет: говорящие размечаются вручную.
- Тест скачивания документов не заменяет визуальную проверку в Microsoft Word.
- Это временный Tunnel с одним общим рабочим пространством, не круглосуточный хостинг.
  Постоянный сервер, многопользовательская авторизация и Supabase не подключены.
- Starlette TestClient выводит одно предупреждение об устаревшей интеграции httpx.
